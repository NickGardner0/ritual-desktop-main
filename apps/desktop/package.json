{
  "name": "@ritual/desktop",
  "private": true,
  "type": "module",
  "scripts": {
    "watcher:build": "cd src-tauri/bin/ritual-watcher && ./build.sh",
    "shell:dev": "npm --prefix ../.. run --workspace @ritual/chat-runtime build && npm --prefix ../desktop-ui run dev",
    "shell:build": "npm --prefix ../.. run --workspace @ritual/chat-runtime build && mkdir -p src-tauri/resources/chat-runtime && cp -R ../../packages/chat-runtime/dist/. src-tauri/resources/chat-runtime/ && npm --prefix ../desktop-ui run build",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "npm run watcher:build && tauri build"
  }
}
