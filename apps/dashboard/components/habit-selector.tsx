'use client';

/**
 * This file re-exports CommandPalette from the new location.
 * Kept for backward compatibility with existing imports.
 */

export { default, HabitSelector } from './command-palette';
