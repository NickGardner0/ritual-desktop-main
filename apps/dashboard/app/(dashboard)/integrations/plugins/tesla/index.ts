import { buildCard } from './card';
import { DetailPanel } from './detail-panel';
import { useTeslaIntegration } from './use-tesla-integration';

export const id = 'tesla';
export const detailKey = 'tesla';
export const title = 'Tesla';
export const keywords = ['car', 'vehicle', 'driving', 'miles'];

export { buildCard, buildCard as Card, DetailPanel, useTeslaIntegration, useTeslaIntegration as useIntegration };
